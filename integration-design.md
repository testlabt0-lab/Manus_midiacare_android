# تصميم تكامل المواعيد وتسجيل الدخول والإشعارات

## نطاق التكامل

يتصل تطبيق MediCare Pro Mobile بخادم المواعيد عبر واجهة HTTPS فقط. لا يصل العميل إلى قاعدة بيانات الخادم ولا يحتفظ بأي سر خادمي. يظل السجل المحلي متاحاً قبل تسجيل الدخول أو عند تعذر الشبكة، بينما تصبح الزيارات والتنبيهات المصرح بها مرئية بعد نجاح جلسة المريض.

| المجال | القرار التصميمي |
| --- | --- |
| عنوان الخدمة | `EXPO_PUBLIC_PATIENT_API_ORIGIN`؛ إن لم يُضبط يستخدم التطبيق العنوان الموثق للخادم الحالي |
| تسجيل الدخول | رمز تفويض أحادي الاستخدام مع PKCE، يبدأ في متصفح النظام ويعود إلى مخطط التطبيق `medicarepro://auth` |
| تخزين الجلسة | زوج رموز وصول وتجديد في `expo-secure-store` على الجهاز؛ لا تحفظ الرموز في AsyncStorage |
| نطاق الصلاحيات | استدعاءات المريض فقط: قراءة الزيارات، إنشاء زيارة، قراءة التنبيهات، وتعليم التنبيه كمقروء |
| المزامنة | تجديد رمز الوصول قبل العملية، وجلب الزيارات والتنبيهات بعد نجاح الجلسة أو عند طلب المستخدم التحديث |
| الإشعارات | إنشاء قناة Android، طلب الإذن، جدولة تذكير الموعد محلياً، وعرض التنبيهات الواردة عند مزامنتها؛ يسجل رمز Expo لدى الخادم بأفضل جهد عندما تكون الواجهة متاحة |

## عقد البيانات

| المورد | حقول الخادم المستخدمة | تحويل التطبيق |
| --- | --- | --- |
| الزيارة | `reference`، `clinicName`، `serviceName`، `districtLabel`، `scheduledStart`، `state` | تحويل الحالة إلى شارة الزيارة والموعد إلى طابع زمني محلي |
| تنبيه المريض | `id`، `visitId`، `kind`، `title`، `body`، `readAt`، `createdAt` | إبقاء حالة القراءة ومحتوى التنبيه كما أصدرهما الخادم |
| جلسة المريض | `access_token`، `refresh_token`، `expires_in` | حساب وقت انتهاء صالح وتجديد الرمز قبل انقضائه بدقيقة |
| رمز الجهاز | Expo Push Token | يرسل عند وجود جلسة؛ فشل التسجيل لا يلغي جدولة التذكيرات المحلية |

## تدفق المستخدم

يسجل المريض الدخول من شاشة الحساب. يفتح التطبيق تفويض الخادم في متصفح النظام مع PKCE، ثم يستبدل رمز العودة بجلسة قصيرة العمر ويحفظها في التخزين الآمن. عند العودة، يجلب التطبيق الزيارات والتنبيهات الخاصة بالمريض ويسجل تذكيرات الموعد المحلية. يمكن للمريض عندئذٍ إنشاء طلب زيارة مرتبط بالخادم، ومزامنة البيانات يدوياً، وتسجيل رمز الجهاز عندما يمنح إذن الإشعارات.

> تظل الإشعارات بعيدة عن التفاصيل الطبية الحساسة؛ تعرض تذكيراً بموعد أو تحديث حالة فقط، دون نتائج أو وصفات أو تشخيصات على شاشة القفل.
