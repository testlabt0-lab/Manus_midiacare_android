# ربط MediCare Pro Mobile بتطبيق الويب

## النتيجة المستهدفة

يرتبط تطبيق أندرويد بخادم تطبيق الويب عبر واجهة API واحدة، وليس بقاعدة البيانات مباشرة. وبهذه الطريقة تصبح الزيارات، عضويات العيادات، التقارير، الفواتير، والتنبيهات صادرة من مصدر واحد مع بقاء فحص الصلاحيات في الخادم.

> **قاعدة أساسية:** لا يُحفظ رابط قاعدة البيانات أو مفتاح توقيع الجلسة أو أي سر خادمي داخل تطبيق أندرويد. يتصل الهاتف بواجهة API المنشورة فقط، ويرسل رمز وصول قصير العمر بعد تسجيل دخول آمن.

## ما هو متاح حالياً في تطبيق الويب

الخادم يوفّر بالفعل واجهة typed API للبيانات التالية:

| واجهة API | الصلاحية | استخدام تطبيق أندرويد |
| --- | --- | --- |
| `auth.me` | عامة | معرفة المستخدم الحالي بعد نجاح تسجيل الدخول. |
| `visits.listMine` و`visits.getMine` | مستخدم مسجل | عرض زيارات صاحب الحساب فقط. |
| `visits.create` | مستخدم مسجل | إرسال طلب زيارة جديدة من الهاتف. |
| `memberships.mine` | مستخدم مسجل | عرض العيادات والأدوار المرتبطة بالحساب. |
| `visits.listOperations` و`assign` و`transition` | مدير مخول | لوحة تشغيل العيادة وتحديث الحالة وفق قواعد الخادم. |
| `outputs.reportMine` و`outputs.invoiceMine` | صاحب الزيارة المخول | الوصول إلى التقرير أو الفاتورة بعد التحقق. |

المصادقة الخادمة تقبل جلسة موقعة في رأس `Authorization: Bearer <token>`، لكن تطبيق الويب الحالي ينشئها في تدفق OAuth يعتمد على ملف تعريف ارتباط للمتصفح. لذلك يحتاج تطبيق أندرويد إلى **تدفق تسجيل دخول خاص بالهاتف**؛ ولا يصح نسخ ملف تعريف الارتباط أو إنشاء رمز موقّع في التطبيق.

## تدفق تسجيل الدخول المقترح للهاتف

| المرحلة | ما يحدث | الضبط الأمني المطلوب |
| --- | --- | --- |
| 1. بدء الدخول | يفتح التطبيق جلسة متصفح نظام إلى صفحة دخول تطبيق الويب، مع رابط رجوع عميق خاص بالتطبيق. | استخدم حالة عشوائية وPKCE واربطها بالطلب الذي بدأ من الهاتف. |
| 2. الدخول في المتصفح | ينفّذ المستخدم OAuth عبر صفحة الويب الحالية. | يبقى ملف تعريف الارتباط في المتصفح فقط؛ لا يمر إلى تطبيق الهاتف. |
| 3. رمز أحادي الاستعمال | بعد نجاح الدخول، يصدر الخادم رمز تفويض قصير العمر ومربوطاً بالمستخدم والجهاز. | خزّن **بصمة** الرمز في الخادم، واجعله ذا استخدام واحد وانتهاء سريع. |
| 4. الرجوع للتطبيق | يعيد المتصفح فتح رابط عميق مثل `medicarepro://auth?code=...&state=...`. | تحقق من `state` في الهاتف قبل استبدال الرمز. |
| 5. استبدال الرمز | يرسل الهاتف الرمز وPKCE verifier إلى الخادم لاستلام access token قصير العمر. | لا تُرجع الجلسة المخصصة للويب ولا مفتاحاً طويل العمر. |
| 6. التخزين والاستخدام | يخزّن الهاتف access token في التخزين الآمن ويضعه في رأس كل طلب API. | استخدم التخزين الآمن للجهاز، وجدّد الرمز أو أعد الدخول عند انتهاء صلاحيته. |

يوفر Expo APIs مناسبة لفتح جلسة دخول المتصفح والعودة للتطبيق عبر رابط عميق [1]، ولحفظ رمز الوصول في تخزين آمن على الجهاز [2].

## طبقة الاتصال في تطبيق أندرويد

أضف في مشروع الهاتف حزم عميل typed API وطبقة المصادقة، ثم اجعل عنوان الخادم متغير بيئة للبناء، مثل `EXPO_PUBLIC_API_URL=https://<domain>/api/trpc`. لا تضف عنواناً محلياً أو ثابتاً داخل `App.tsx`.

```text
MediCare Pro Mobile
        │  Authorization: Bearer <short-lived token>
        ▼
https://<published-web-domain>/api/trpc
        │  تحقق من الجلسة + الدور + نطاق العيادة
        ▼
قاعدة بيانات MediCare Pro المشتركة
```

بعد تسجيل الدخول، تتصرف الشاشة وفق صلاحية المستخدم القادمة من `auth.me` و`memberships.mine`:

| دور المستخدم | شاشات الهاتف المناسبة |
| --- | --- |
| مريض / مستخدم | زياراتي، طلب زيارة، تنبيهات المواعيد، التقارير والفواتير المخولة. |
| مدير عيادة | قائمة التشغيل، تعيين الفريق، انتقالات الحالات المسموحة، التنبيهات التشغيلية. |
| عضو فريق | المهام المكلف بها فقط، وتحديث المرحلة التي يسمح بها الخادم. |

يجب أن تبقى كل عملية حساسة في الخادم. إخفاء زر من واجهة الهاتف لا يساوي منعاً للصلاحية؛ يجب أن يعيد الخادم رفض أي طلب غير مخول، كما يفعل حالياً لإجراءات التشغيل.

## مزامنة التنبيهات

يشترك التطبيقان في جدول تنبيهات خادمي مستقبلي أو يستخرجان التنبيه من أحداث الزيارات. عند تغيير حالة زيارة أو إنشاء موعد، ينشئ الخادم سجلاً عاماً للتنبيه ويرسله للمستخدم أو أعضاء العيادة المخولين. ثم يسحب الهاتف القائمة عبر API ويحوّل التنبيه إلى إشعار نظام إذا وافق المستخدم.

| نوع التنبيه | مصدره الصحيح | النص الخارجي الآمن |
| --- | --- | --- |
| موعد جديد أو تغير وقته | خادم الزيارات | «لديك تحديث بخصوص موعدك في MediCare Pro». |
| تغيير حالة زيارة | خادم التشغيل | «تم تحديث حالة زيارة مرتبطة بحسابك». |
| رسالة معلوماتية من العيادة | خادم تنبيهات مع صلاحيات | «لديك رسالة جديدة من العيادة». |

لا تضع تشخيصاً أو نتيجة فحص أو تعليمات علاجية مفصلة في إشعار الشاشة المقفلة. يُفتح المحتوى التفصيلي بعد تحقق الهوية والصلاحية داخل التطبيق.

## ما سيتغير برمجياً عند البدء بالتنفيذ

| المشروع | التعديلات المطلوبة |
| --- | --- |
| تطبيق الويب | إضافة مسار تفويض مخصص للهاتف، وجدول/تخزين رموز التفويض الأحادية، وإجراءات تبادل وتجديد الرمز، مع السماح الآمن بالطلبات من تطبيق أندرويد. |
| تطبيق أندرويد | إضافة تسجيل دخول المتصفح والرابط العميق والتخزين الآمن وعميل typed API وحالات تحميل/انقطاع الشبكة. |
| قاعدة البيانات | إضافة جداول رموز التفويض وتواريخ انتهاءها، ثم جدول تنبيهات خادمي إذا أردت مزامنة التنبيهات بين الأجهزة. |
| الاختبارات | اختبار رمز أحادي الاستعمال، انتهاء الصلاحية، رفض الأدوار غير المخولة، ومزامنة الزيارة والتنبيه من الويب إلى الهاتف. |

## اختبار الربط

يُختبر مركز التنبيهات والواجهات الحالية عبر المتصفح باستخدام:

```bash
pnpm web
```

أما OAuth والرابط العميق والتخزين الآمن وإشعارات النظام، فيجب اختبارها على محاكي أندرويد أو جهاز فعلي؛ فالمتصفح يحاكي الواجهة ولا يثبت تدفق المصادقة أو إشعار النظام الأصلي كاملاً.

## المراجع

[1]: https://docs.expo.dev/guides/authentication/ "Expo authentication guide"
[2]: https://docs.expo.dev/versions/latest/sdk/securestore/ "Expo SecureStore"
[3]: https://trpc.io/docs/client/vanilla/ "tRPC client documentation"
