# ربط حساب المريض بتطبيق MediCare Pro Web

تربط النسخة الحالية من **MediCare Pro Mobile** حساب المريض بالتطبيق المنشور على الرابط التالي:

```text
https://medicarepro-myvdwgyk.manus.space
```

يتصل التطبيق بواجهة الخادم فقط، ولا يصل إلى قاعدة البيانات مباشرة. بعد إكمال الدخول، يحصل الهاتف على رمز وصول قصير العمر ومحدود لنطاق المريض، ثم يستخدمه لقراءة زيارات صاحب الحساب وإنشاء طلب زيارة جديد.

## ما هو موجود في هذه النسخة

| الوظيفة | التنفيذ |
| --- | --- |
| بدء الدخول | يفتح التطبيق متصفح النظام إلى مسار تفويض المريض في تطبيق الويب. |
| حماية الدخول | يستخدم التدفق قيمة `state` وPKCE ورمز تفويض أحادي الاستعمال ينتهي خلال خمس دقائق. |
| العودة للتطبيق | يعود المتصفح إلى `medicarepro://auth`، وهو مسجل في إعداد Android للتطبيق. |
| تخزين الجلسة | يخزن التطبيق زوج رمز الوصول القصير ورمز التجديد في التخزين الآمن للجهاز عند توفره؛ ولا يحفظ أي سر خادمي داخل التطبيق. |
| تجديد الجلسة | قبل إنشاء زيارة أو مزامنة الزيارات/التنبيهات أو تعليمها كمقروءة، يدور التطبيق رمز التجديد عبر `/api/mobile-auth/refresh`. عند الرفض، يمحو الجلسة محلياً ويطلب دخولاً جديداً. |
| نطاق المريض | يعرض تطبيق أندرويد `visits.listMine` و`patientNotifications.listMine` وينشئ `visits.create` ويعلّم تنبيهات المريض فقط كمقروءة. لا يمكن لرمز الهاتف استدعاء إجراءات الإدارة حتى لو كان للحساب دور إداري على الويب. |
| وضع غير متصل | قبل تسجيل الدخول تبقى الزيارات المحلية واضحة ولا تُرسل إلى الخادم. |

## تسلسل الاتصال

```text
تطبيق أندرويد
  │  client_id + state + PKCE challenge
  ▼
تطبيق MediCare Pro Web /api/mobile-auth/start
  │  OAuth في متصفح النظام
  ▼
عودة مؤقتة إلى الخادم /api/oauth/callback
  │  رمز تفويض أحادي الاستعمال
  ▼
medicarepro://auth
  │  code + PKCE verifier
  ▼
تطبيق الويب /api/mobile-auth/token
  │  access + refresh tokens محدودان ونطاقهما patient
  ▼
تطبيق أندرويد → visits.listMine / visits.create / patientNotifications.listMine
  │  عند قرب انتهاء access token
  ▼
تطبيق الويب /api/mobile-auth/refresh → زوج رموز جديد مع إبطال رمز التجديد السابق
```

## إعدادات ضرورية

| الموضع | القيمة |
| --- | --- |
| متغير خادم الويب `MOBILE_WEB_ORIGIN` | `https://medicarepro-myvdwgyk.manus.space` |
| معرف عميل الهاتف | `medicare-pro-mobile-android` |
| مخطط رابط العودة | `medicarepro://auth` |
| حزمة Android | `com.medicarepro.mobile` |
| عنوان API في التطبيق | `https://medicarepro-myvdwgyk.manus.space` |

> **تنبيه أمني:** معرف العميل الثابت يعرّف تطبيق الهاتف منطقياً للخادم، لكنه ليس دليلاً تشفيرياً مستقلاً على أن الطلب صادر من ملف APK أصلي. في إصدار عالي الحساسية، أضف تحققاً من Play Integrity أو آلية إثبات جهاز قبل إصدار الرمز. يبقى PKCE ضرورياً لحماية رمز التفويض من الاعتراض [1].

## اختبار الربط

يمكن معاينة الواجهة عبر المتصفح باستخدام:

```bash
pnpm web
```

تظهر المعاينة زر **تسجيل الدخول** وحالة عدم الاتصال وخيارات السجل المحلي. أما إكمال الدخول الحقيقي فيتطلب تطبيق Android مبنياً أو محاكي أندرويد، لأن المتصفح لا يفتح عادةً مخطط `medicarepro://auth` كما يفعل نظام أندرويد.

بعد تثبيت التطبيق على الجهاز، اختبر بالترتيب التالي:

1. افتح شاشة الحساب واضغط **تسجيل الدخول الآمن**.
2. أكمل الدخول في متصفح النظام.
3. عد إلى التطبيق وتأكد من تحول الحالة إلى **حساب المريض متصل**.
4. افتح **زياراتي** وتحقق من ظهور بيانات الحساب فقط.
5. أضف زيارة مع الحي والموعد، ثم حدّث القائمة للتحقق من وصولها إلى تطبيق الويب.
6. افتح التنبيهات، واقرأ تنبيهاً خادمياً، ثم أعد الفتح للتحقق من حفظ حالة القراءة لحساب المريض نفسه فقط.
7. اترك التطبيق حتى قرب انتهاء رمز الوصول ثم نفّذ مزامنة أو أنشئ زيارة؛ يجب أن تستمر العملية بعد تجديد الجلسة، أو يعود التطبيق إلى وضع غير متصل عند رفض رمز التجديد.
8. حاول استخدام إجراء إداري من تطبيق الهاتف؛ يجب أن يرفضه الخادم بسبب نطاق `patient`.

## حدود المرحلة الأولى

تزامن الزيارات وإنشاؤها وتنبيهات المريض أصبح ضمن نطاق المريض فقط. يظل إرسال إشعارات **النظام** خارج التطبيق غير مفعّل؛ لا يرسل التطبيق نصوصاً تشخيصية أو نتائج أو وصفات إلى شاشة القفل. ولا يزال تحقق OAuth والرابط العميق الكامل بحاجة إلى جهاز Android فعلي أو محاكي.

## المراجع

[1]: https://www.rfc-editor.org/rfc/rfc7636 "Proof Key for Code Exchange by OAuth Public Clients"
[2]: https://docs.expo.dev/guides/authentication/ "Expo Authentication Guide"
[3]: https://docs.expo.dev/versions/latest/sdk/securestore/ "Expo SecureStore"
