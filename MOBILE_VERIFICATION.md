# سجل تحقق معاينة Android عبر الويب

## 25 أغسطس 2026

تأكدت المعاينة على `http://localhost:8081` من ظهور واجهة **MediCare Pro Mobile** في وضع محلي مؤقت، مع إجراءات إضافة زيارة وتسجيل دخول وعلامات تبويب الزيارات والتنبيهات والحساب. لم تسجل وحدة التحكم أخطاء تشغيلية؛ اقتصر الناتج على تنبيه تطويري من React وتنبيه إيقاف استخدام بعض خصائص الظلال في React Native Web.

> لا يثبت هذا التحقق تدفق OAuth والرابط العميق `medicarepro://auth`؛ يلزم جهاز Android فعلي أو محاكي لاختبار ذلك.

كما نجح الأمر `pnpm exec expo export --platform android` بعد التعديل، وأنشأ حزمة Android المصدرية في مجلد `dist` من دون أخطاء تجميع.

بعد إضافة حالة المزامنة، أكدت المعاينة ظهور النص **«سجّل الدخول لمزامنة بيانات حسابك»** ضمن بطاقة الوضع المحلي. ولم تظهر وحدة التحكم أخطاء تشغيلية جديدة؛ بقي فقط تحذير تطوير React Native Web المتعلق بخصائص الظلال.

بعد إضافة حالة الفشل وإعادة المحاولة، استمرت معاينة الويب في عرض التطبيق ووضعه المحلي دون أخطاء تشغيلية. تظل حالة الفشل الفعلية مرتبطة بحساب مريض متصل وتعتمد على فشل طلب مزامنة حقيقي؛ أما منطق الأولوية والنص الآمن فمغطى باختبارات الوحدة.

بعد دمج سجل المزامنة المحلي، بدأت معاينة Expo مع مكتبة التخزين المحلي المتوافقة من دون أخطاء تشغيلية. لا يظهر السجل في الوضع المحلي قبل اتصال حساب المريض، وهو سلوك مقصود لحصره في الحساب المتزامن. بقي فقط تحذير React Native Web المتعلق بخصائص الظلال.

بعد إضافة إعداد الخصوصية، استمرت المعاينة في العمل من دون أخطاء تشغيلية. يظهر مفتاح التحكم ضمن شاشة الحساب بعد ربط الحساب، أما منطق تعطيل السجل وحذفه ومنع إنشاء نتائج جديدة فمغطى باختبارات الوحدة. بقي فقط تحذير React Native Web المتعلق بخصائص الظلال.

بعد إضافة صفحة معلومات الخصوصية، استمرت المعاينة في العمل من دون أخطاء تشغيلية. تتاح الصفحة من شاشة الحساب وتعرض محتوى ثابتاً محلياً فقط؛ ولا تتطلب قراءة أي بيانات مريض أو زيارة. بقي فقط تحذير React Native Web المتعلق بخصائص الظلال.

بعد إضافة مدد الاحتفاظ، استمرت المعاينة في العمل من دون أخطاء تشغيلية. تظهر خيارات الاحتفاظ فقط بعد تفعيل سجل المزامنة وربط الحساب، وتبقى تنقية النتائج الأقدم مغطاة باختبارات الوحدة. بقي فقط تحذير React Native Web المتعلق بخصائص الظلال.

بعد إضافة حذف البيانات المحلية، استمرت المعاينة في العمل من دون أخطاء تشغيلية. يظهر الإجراء ضمن إعدادات الخصوصية ويطلب تأكيداً قبل التنفيذ؛ وتبقى نتيجة الحذف الفعلية مرتبطة بتفاعل المستخدم على جهاز Android، بينما نطاق المفاتيح المحلية والنص التحذيري مغطى باختبارات الوحدة. بقي فقط تحذير React Native Web المتعلق بخصائص الظلال.

بعد إضافة شاشة فحص الاتصال، استمرت المعاينة في العمل من دون أخطاء تشغيلية. تظهر مؤشرات التشخيص الفعلية بعد فتح الشاشة من الحساب، وتعتمد على حالة الجلسة والمزامنة المحلية فقط، بينما منطق الحالات الآمنة وإجراء التحديث أو تسجيل الدخول مغطى باختبارات الوحدة. بقي فقط تحذير React Native Web المتعلق بخصائص الظلال.

بعد إضافة تحديث الحساب عند عودة التطبيق للمقدمة، استمرت المعاينة في العمل من دون أخطاء تشغيلية. الإعداد اختياري ومحلي، ولا يستخدم مؤقتات أو عملاً مجدولاً؛ ويغطي اختبار الوحدة شروط وجود جلسة وعدم وجود مزامنة جارية قبل بدء التحديث. بقي فقط تحذير React Native Web المتعلق بخصائص الظلال.

بعد إضافة قائمة تحقق الإصدار، استمرت المعاينة في العمل من دون أخطاء تشغيلية. تفرق القائمة بوضوح بين التحقق المنجز في بيئة التطوير والعناصر التي ما زالت تتطلب جهاز Android أو توقيع APK قبل التوزيع؛ ولا تدعي إتمام اختبار جهاز فعلي. بقي فقط تحذير React Native Web المتعلق بخصائص الظلال.

بعد إضافة متابعة عناصر الإصدار، تم فتح قائمة تحقق APK من شاشة الحساب بنجاح في معاينة الويب. ظهرت عناصر الجهاز والتوزيع قابلة للتأشير المحلي مع نص صريح يفيد بأن التأشير لا يمثل تحققاً آلياً؛ وظلت عناصر التحقق المنجز في بيئة التطوير غير قابلة للتعديل. لا يزال اختبار التأشير على جهاز Android فعلي مطلوباً قبل الإصدار.

بعد تحسين استعادة الجلسة، فتحت معاينة الويب شاشة الحساب وظهرت الحالة المحلية بوضوح تحت عنوان «لا توجد جلسة محفوظة» مع رسالة آمنة تطلب تسجيل الدخول للمزامنة. لا يمكن لمحاكاة الويب إثبات سلوك تجديد الرمز عند استرجاع جلسة محفوظة؛ يلزم جهاز Android أو محاكي لاختبار ذلك.

تحقق الإصدار الأخير: نجحت اختبارات Vitest (37 اختباراً ضمن 14 ملفاً)، ونجح `tsc --noEmit`، ونجح `expo export --platform android`. لم تظهر في معاينة الويب أخطاء تشغيلية؛ ظهر فقط تحذير تطوير من React Native Web حول خصائص `shadow*` القديمة. يبقى اختبار OAuth والرابط العميق واستعادة SecureStore على جهاز Android أو محاكي فعلي مطلوباً.

بعد توضيح مسار إعادة محاولة استعادة الجلسة، فتحت شاشة الحساب في معاينة الويب بنجاح وبقيت حالة عدم وجود جلسة محفوظة ظاهرة بشكل صحيح. تغطي اختبارات الوحدة حالة الجلسة المحفوظة المتعذر التحقق منها مؤقتاً؛ ويلزم جهاز Android أو محاكي مع جلسة فعلية للتحقق من النص والزر في هذا المسار الأصلي.

تحقق التحسين الإضافي: نجحت اختبارات Vitest (38 اختباراً ضمن 14 ملفاً)، ونجح `tsc --noEmit` وتصدير Android. لا تعرض معاينة الويب أخطاء تشغيلية جديدة؛ ظل تحذير خصائص `shadow*` التطويري فقط.

بعد إضافة ملخص الموعد القادم، ظهرت البطاقة في لوحة معاينة الويب مع حالة عدم وجود موعد مستقبلي مؤكد أو نشط. لم تظهر أخطاء تشغيلية في سجل المعاينة؛ اقتصر الناتج على تحذير `shadow*` التطويري المعتاد. يغطي اختبار الوحدة اختيار أقرب موعد مستقبلي صالح وتجاهل الزيارات الملغاة والمكتملة والماضية.

نجح التحقق النهائي لملخص الموعد القادم: **40 اختباراً** ضمن 14 ملفاً، وفحص TypeScript، وتصدير Android. لا يحمل الملخص بيانات تشخيصية أو نتائج أو وصفات؛ يعرض نوع الخدمة وموعد الزيارة المصرح بها فقط عندما يتوفر موعد صالح.

بعد إضافة مرشحات حالة الزيارات، فتحت معاينة الويب تبويب «زياراتي» وظهرت مرشحات «الكل»، و«النشطة»، و«المكتملة»، و«الملغاة» بوضوح، مع بقاء حالة عدم وجود زيارات المحلية سليمة. لم تظهر أخطاء تشغيلية أثناء هذا التحقق المرئي.

نجح التحقق النهائي للمرشحات: **41 اختباراً** ضمن 14 ملفاً، وفحص TypeScript، وتصدير Android. لم يسجل سجل المعاينة أخطاء تشغيلية؛ ظهر فقط تحذير تطوير React Native Web المتعلق بخصائص `shadow*`.

بعد إضافة البحث المحلي، فتحت معاينة الويب تبويب «زياراتي» وظهر حقل «ابحث باسم العيادة أو نوع الخدمة» فوق مرشحات الحالة. لم يُرسل الحقل أي طلب شبكة في المعاينة لأنه يرشح المجموعة الموجودة في التطبيق فقط.

نجح التحقق النهائي للبحث المحلي: **42 اختباراً** ضمن 14 ملفاً، وفحص TypeScript، وتصدير Android. يقتصر البحث على اسم العيادة ونوع الخدمة من الزيارات الموجودة محلياً أو المتزامنة المصرح بها؛ ولا يبحث في أي تشخيص أو نتيجة أو وصفة.
